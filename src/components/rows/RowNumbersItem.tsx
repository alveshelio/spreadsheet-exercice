import * as React from "react"

import styled from "../../templages/styled"

const RowNameItem = styled.div`
  color: ${({ theme }) => theme.primary};
  background: #5a5a5a;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
`

export default RowNameItem
